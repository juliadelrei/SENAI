let pessoa = {
    nome:"julia", idade:15, altura:1.55, estudante: true };
    let frase = `Meu nome é ${pessoa.nome}, tenho ${pessoa.idade} anos, ${pessoa.altura}m de altura e sou estudante: ${pessoa.estudante}.`;

console.log(frase);